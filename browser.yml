name: browser
category: headunit

identifier:
    msb: 0x00
    lsb: 0x49

api:
    intro: 4
    update: 11

commands:
  - name: load_url
    mandatory: [0x01]

properties:
  - id: 0x01
    name: url
    type: string
    pretty_name: URL
    description: The UTF8 bytes of the URL string
